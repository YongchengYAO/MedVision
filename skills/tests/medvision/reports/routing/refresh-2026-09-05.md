# Routing handoff — `medvision` refresh, 2026-09-05

**Assignments preserved.** No reclassification.

| Field | Value |
| --- | --- |
| `repo_id` | `YongchengYAO/MedVision` (unchanged) |
| `skill_id` | `medvision` (unchanged) |
| `taxonomy_sha256` | `f8c306386015711634ddbb43a5eb95d1f58909c3513ce2063ba42efdd583a431` (unchanged) |
| Areas / families | LLM Models, Training, and Alignment → Model Evaluation Benchmarks; Biomedical AI → Medical Object Detection; LLM Models, Training, and Alignment → LLM Fine-Tuning |

Rationale: the refresh changed dependency-behaviour wording, one package-data claim and the
provenance stamp. It added, removed and moved no capability, so the capability scope backing the
three area-family assignments is identical to the classified build.

`skills/disco/routing_decision/classification.json` was updated as part of this refresh:
`source_commit` now reads `780e247…` and `skill_content_sha256` is the real digest of the refreshed
runtime tree, `sha256:40d4651abd018b54bc3c6ecca2025ca64984c1068874869be57b5ba1519d29fc` (previously
an all-zero placeholder), computed with the importer's own `digestPortableTree()` algorithm.
Recompute it if the runtime tree changes again before an import:

```bash
REPO_ROOT="$(git rev-parse --show-toplevel)"
python "$REPO_ROOT/skills/tests/medvision/reports/routing/compute_skill_digest.py" \
    "$REPO_ROOT/skills/medvision" \
    --write "$REPO_ROOT/skills/disco/routing_decision/classification.json"
```

The skill remains **not imported**, as decided at creation. Nothing in this refresh changes that;
importing is a separate, explicit step.
