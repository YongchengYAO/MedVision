# Routing handoff — refresh of 2026-09-04

Decision: **assignments retained**, no reclassification.

- Repository change: commit `980e9df` reworks how the SFT stage names and hands over its prepared
  dataset. It adds no capability and removes none; the three area-family assignments
  (`LLM Models, Training, and Alignment / Model Evaluation Benchmarks`,
  `Biomedical AI / Medical Object Detection`,
  `LLM Models, Training, and Alignment / LLM Fine-Tuning`) describe the same repository scope.
- Taxonomy hash unchanged (`f8c30638…`).
- `classification.json` updated in place: `source_commit` -> `980e9dfb4567d6b335590103f6370d87ae1a3532`;
  README evidence ranges shifted by the two-line changelog insertion (`691-722`, `875-902`); the model
  registry range tightened to the dictionary's actual extent (`19-65`); the `sft_utils.py` range
  `2696-2960` re-verified (`prepare_trainer` at 2696, `prepare_trainer_fullFT` at 2912).
- `references/repo-routing-metadata.json` (runtime, minimal v2) untouched.
- `skill_content_sha256` remains the zero placeholder and must be recomputed right before import.
- Import status: **not imported** — the maintainer declined import at creation and only asked for the
  skill to be updated. The refreshed runtime tree stays staged at `skills/medvision`.
