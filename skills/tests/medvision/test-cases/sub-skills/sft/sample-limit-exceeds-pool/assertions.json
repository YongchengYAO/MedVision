{
  "schema": "disco.usability-case.v1",
  "target_skill_area": "sft",
  "target_capability": "sample-limit resolution and validation carve-out",
  "difficulty": "intermediate",
  "evidence_basis": [
    "src/medvision_bm/sft/sft_utils.py",
    "script/sft/train__SFT-CoT__Qwen2.5VL7B__D110k-AD5.5k-TL5.5k__512x512.sh"
  ],
  "expected_skill_files": [
    "sub-skills/sft/references/data-preparation.md",
    "sub-skills/sft/scripts/check_sample_limits.py"
  ],
  "assertions": [
    "The response states that validation is carved out first, per task, grouped by source volume.",
    "The response explains that a limit above the pool bootstraps with replacement rather than erroring.",
    "The response distinguishes draw count from distinct-sample count.",
    "The response warns that a global cap below the per-task sum truncates silently.",
    "The response uses the bundled sample-limit checker to show the resolved numbers."
  ]
}
