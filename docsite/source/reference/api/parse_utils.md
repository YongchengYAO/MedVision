# `parse_utils`

Geometry metrics (IoU, F1, precision, recall), measurement metrics (MAE, MRE), and the helpers that extract numeric answers from raw model output.

```{eval-rst}
.. automodule:: medvision_bm.utils.parse_utils
   :members:
```
