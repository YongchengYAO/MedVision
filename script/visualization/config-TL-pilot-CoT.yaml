# Config for the pilot-study T/L radar: MedVision-V0 vs frontier API models on the
# limit100 subset (Results/MedVision-TL-CoT-limit100/, 750 samples). MedVision-V0 first.
# GPT-5.5-Pro's run stopped at 490 samples (API budget exhausted), so its summary lacks two
# targets; the exporter's union-spoke rule for TL-Pilot renders those as gaps, not dropped spokes.
model_display_name:
  "MedVision__fullRFT__qwen25vl-7b-fullSFT__AD-TL-D__512x512__PRxAnswer_s250": "MedVision-V0 (7B)"
  "Claude-Fable-5": "Claude-Fable-5"
  "Gemini-3.1-Pro": "Gemini-3.1-Pro"
  "GPT-5.5-Pro": "GPT-5.5-Pro"

