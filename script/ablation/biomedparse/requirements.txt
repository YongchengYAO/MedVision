--extra-index-url https://download.pytorch.org/whl/cu124

# Core numeric stack (keeps NumPy < 2 to avoid ABI issues)
numpy==1.26.4
packaging==23.0
setuptools==65.6.3
ninja==1.11.1.1

# PyTorch (CUDA 12.4)
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124

# Data preparation
opencv-python-headless
nibabel
Pillow
tqdm
pydicom
SimpleITK
pynrrd
scipy
scikit-image
matplotlib

# Model & training
pandas==2.2.2
scikit-learn==1.4.2
hydra-core==1.3.2
lightning==2.3.0
marshmallow==3.23.2
timm==0.9.16
deepspeed==0.14.2
transformers==4.40.0
open-clip-torch==2.26.1
sentencepiece==0.2.0
kornia==0.7.3
python-dotenv==1.0.1
safetensors
# <1.0 required by transformers 4.40 / tokenizers 0.19; >=0.35.3 required by medvision_ds
huggingface-hub==0.36.0
accelerate
datasets
psutil

# MedVision dataset access (medvision_bm.sft.sft_utils)
datasets==3.6.0

# NOTE: detectron2 is NOT pip-installable; setup.sh builds it from source
# (pip install git+https://github.com/facebookresearch/detectron2.git).
# This requires NVCC (CUDA toolkit) and takes ~10-20 min on first install.
